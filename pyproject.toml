[project]
name = "data-prep"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Larissa Ferreira", email = "larissadferr@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
data-prep = "data_prep:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
